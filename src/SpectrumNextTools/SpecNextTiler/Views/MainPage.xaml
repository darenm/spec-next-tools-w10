<Page
    x:Class="SpecNextTiler.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SpecNextTiler.Views"
    xmlns:designtime="using:SpecNextTiler.ViewModel.Design"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:models="using:SpecNextTiler.Models" xmlns:controls="using:SpecNextTiler.Controls" xmlns:controls1="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <d:Page.DataContext>
        <designtime:MainPageViewModel />
    </d:Page.DataContext>

    <Grid>
        <controls1:DockPanel x:Name="TileDesigner" 
                          Background="LightGray" 
                          LastChildFill="True" 
                          Margin="2" 
                          Padding="0,0,0,0">
            <Grid Width="236" controls1:DockPanel.Dock="Right">
                <controls:PaletteControl/>
            </Grid>
            <Grid x:Name="PixelSurface">
                
            </Grid>
        </controls1:DockPanel>

    </Grid>
</Page>
